function Main() {
    return (
        <main>
            <form>
                <input type="text" placeholder="e.g. oregano" aria-label="Add ingredients" />
                <button>Add ingredient</button>
            </form>
        </main>
    );
}

export default Main;
